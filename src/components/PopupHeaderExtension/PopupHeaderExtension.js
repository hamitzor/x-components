import PropTypes from 'prop-types';

const PopupHeaderExtension = props => props.children;

PopupHeaderExtension.propTypes = {
   children: PropTypes.any.isRequired
};

export { PopupHeaderExtension };
